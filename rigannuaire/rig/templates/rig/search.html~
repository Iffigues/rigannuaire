{% extends 'rig/layout.html' %}
{% load static %}
{% block title %}
search
{% endblock %}
{% block content %}
<div class="row" id="searcher" >
  <form class="form-inline" id="hahaer"  action="{% url 'rig:s' %}" method="post">
    {% csrf_token %}
    <div clas="form-group">
      <label for="{{ form.fname.id_for_label }}">
	{{ form.fname }}
	<label for="{{ form.lname.id_for_label }}">	 
	  {{ form.lname }}
	  <label for="{{ form.email.id_for_label }}">
	    {{ form.email }}
    </div>
    <div class="form-group">
      <label for="{{ form.snbr.id_for_label }}">
	{{ form.snbr}}
	<label for="{{ form.sn.id_for_label }}">
	  {{ form.sn }}
	  <label for="{{ form.st.id_for_label }}">
	    {{ form.st }}
    </div>
    <div class="form-group">
      <label for="{{ form.city.id_for_label }}">
	{{ form.city }}
	<label for="{{ form.sigle.id_for_label }}">
	  {{ form.sigle}}
	  <label for="{{ form.zips.id_for_label }}">
	    {{ form.zips }}
    </div>
    <div class="form-goup">
      <label for="{{ form.tel.id_for_label }}">
	{{ form.tel }}
	<label for="{{ form.gender.id_for_label }}">
	  {{ form.gender }}
	  <button type="submit" class="btn btn-light btn-block">chercher</button>
	  
    </div>
  </form>   
</div>
{% block searcher %}{% endblock %}
{% endblock %}
